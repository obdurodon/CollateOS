2014-07-02 meeting notes

Minas will do:

Diagnostic dump of suffix array plus JSON output will be converted to raw UTF-8, instead of u'\u0000\ escapes

Integrate preprocessing into the workflow (but not postprocessing); output will be XML

David will do:

Create pvl-tei.xml with <w> tags for unified input

